        Set<List<Integer>> output = new HashSet<>();

        int r = nums.length-1;
        int l = 0;
        int target = 0;
        
    public List<List<Integer>> threeSum(int[] nums) {
class Solution {
        Arrays.sort(nums);
