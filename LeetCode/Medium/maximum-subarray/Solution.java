                cursum = temp;
            }

            if(cursum > maxsum){
                maxsum = cursum;
            }
            }else{
                cursum = nums[i];
            if(temp < nums[i]){
            int temp = cursum + nums[i];

        for(int i=0;i<nums.length;i++){
        }
        return maxsum;
    }
}
