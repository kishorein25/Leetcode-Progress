                if (height[left] >= leftM) {
                    leftM = height[left];
                } else {
                    water = water + (leftM - height[left]);
                }

            if (height[left] < height[right]) {
        while (left < right) {

        int water = 0;
